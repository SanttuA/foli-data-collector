services:
  foli-harvester:
    build: .
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/app/data
    command: ["python", "-m", "foli_harvester", "collect"]
